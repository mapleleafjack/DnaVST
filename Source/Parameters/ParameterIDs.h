#pragma once

#include <juce_core/juce_core.h>

namespace ParamIDs
{
    const juce::String heightGain = "heightGain";
    const juce::String rotation = "rotation";
    const juce::String zoom = "zoom";
    const juce::String thickness = "thickness";
}
